<p id="notice"><%= notice %></p>

<h1>Listing Interventions</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Duration</th>
      <th>Calendar</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @interventions.each do |intervention| %>
      <tr>
        <td><%= intervention.name %></td>
        <td><%= intervention.duration %></td>
        <td><%= intervention.calendar.name %></td>
        <td><%= link_to 'Show', [:admin, intervention] %></td>
        <td><%= link_to 'Edit', edit_admin_intervention_path(intervention) %></td>
        <td><%= link_to 'Destroy', [:admin, intervention], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Intervention', new_admin_intervention_path %>
